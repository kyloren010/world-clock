<!DOCTYPE html>
<html>

<head>
    <title>Live Weather</title>
    <style>
        body {
            background-image: url("vidar-nordli-mathisen-Vl5Awbq9jx0-unsplash.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            margin: 0;
            padding: 0;
        }

        .container {
      display: relative;
      height: 100%;
    }

    .chart-container {
      position: absolute;
      top : 25px;
      right: 20px;
    }

    canvas {
      border: 1px solid black;
    }

        #container {
            display: flex;
            flex-direction: column;
            position: absolute;
            left: 50px; /* Position the container to the left */
            top: 15px; /* Position the container from the top */
            width: 100%; /* Set the container width to 100% of its parent */
            height: 100vh;
            color: #fff;
            text-align: left;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            /* font-family:'Times New Roman', Times, serif; */
            font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        #result {
            margin-top: 1rem;
            font-size: 1.5rem;
            white-space: pre;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        #go-back-button {
            position: absolute;
            top: 10px;
            left: 10px;
            display: flex;
            align-items: center;
            background-color: transparent;
            border: none;
            cursor: pointer;
            color: #fff;
            font-size: 1rem;
            text-decoration: none;
        }
        
        #go-back-button img {
            width: 20px;
            height: 20px;
            margin-right: 5px;
        }
    </style>
</head>

<body>
    <div id="container">
        <h1>Live Weather</h1>
        <div id="result"></div>
    </div>

    <div class="container">
        <div class="chart-container">
          <canvas id="myChart" width="600" height="600"></canvas>
        </div>
      </div>

    <a onclick="goBack()" id="go-back-button">
        <img src="goback.jpg" alt="Back Icon">
        Go Back
    </a>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="weather.js"></script>
</body>
</html>